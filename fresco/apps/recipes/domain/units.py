from enum import Enum


class UnitsEnum(Enum):
    """
    Enum that gives the units for the ingredients
    """

    gram = "g"
    kilogram = "kg"
    litres = "l"
    mililitres = "ml"
    unit = "unit"
